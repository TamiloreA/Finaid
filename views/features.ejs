<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FinAid - Features</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <style>
        .mobile-menu {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            background-color: #000;
            transform: translateY(-100%);
            transition: transform 0.3s ease-in-out;
            z-index: 40;
          }
      
          .mobile-menu.active {
            transform: translateY(0);
          }

        /* Modern navbar */
        .navbar-modern {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            position: fixed;
            width: 100%;
            z-index: 100;
            transition: all 0.3s ease;
        }

        .navbar-modern.scrolled {
            background: rgba(255, 255, 255, 0.98);
            box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
        }

        .btn-modern {
            background-color: #000;
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 0.375rem;
            font-weight: 500;
            transition: background-color 0.3s;
          }
      
          .btn-modern:hover {
            background-color: #333;
          }

          /* Existing styles remain unchanged */
          .feature-card {
            background: white;
            border-radius: 12px;
            padding: 2rem;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, #000 0%, #333 100%);
            transition: all 0.6s ease;
        }

        .feature-card:hover::before {
            left: 0;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        }

        .feature-icon {
            width: 60px;
            height: 60px;
            background: #f8f8f8;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1.5rem;
            transition: all 0.4s ease;
        }

        .feature-card:hover .feature-icon {
            transform: scale(1.1) rotate(5deg);
            background: #000;
            color: white;
        }

        .animated-number {
            font-size: 3.5rem;
            font-weight: bold;
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.6s ease;
        }

        .animated-number.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .feature-grid {
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.8s ease forwards;
        }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .parallax-section {
            background-attachment: fixed;
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
            padding: 150px 0;
            position: relative;
        }

        .parallax-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.7);
        }

        .floating {
            animation: floating 3s ease-in-out infinite;
        }

        @keyframes floating {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
            100% { transform: translateY(0px); }
        }

        .progress-bar {
            height: 4px;
            background: #f0f0f0;
            border-radius: 2px;
            overflow: hidden;
            margin: 1rem 0;
        }

        .progress-bar-fill {
            height: 100%;
            background: #000;
            width: 0;
            transition: width 1.5s ease;
        }

        /* New animations for statistics */
        .stat-card {
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.6s ease;
        }

        .stat-card.visible {
            opacity: 1;
            transform: translateY(0);
        }

        @keyframes countUp {
            from {
                content: "0";
            }
        }

        .feature-comparison {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin-top: 4rem;
        }

        .comparison-card {
            background: white;
            padding: 2rem;
            border-radius: 12px;
            text-align: center;
            transition: all 0.3s ease;
        }

        .comparison-card:hover {
            transform: scale(1.05);
        }

        /* New section styles */
        .testimonial-card {
            background: white;
            padding: 2rem;
            border-radius: 12px;
            transition: all 0.3s ease;
            opacity: 0;
            transform: translateY(20px);
        }

        .testimonial-card.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .timeline-item {
            opacity: 0;
            transform: translateX(-20px);
            transition: all 0.6s ease;
        }

        .timeline-item.visible {
            opacity: 1;
            transform: translateX(0);
        }

        .main-content {
            padding-top: 72px;
        }

        @media (max-width: 768px) {
            .feature-comparison {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 768px) {
            .desktop-menu {
                display: none;
            }

            .mobile-menu-btn {
                display: block;
            }

            .hero-text {
                text-align: center;
                padding: 0 1rem;
            }

            .hero-buttons {
                justify-content: center;
            }
        }

        /* Hamburger Menu */
        .hamburger {
            width: 30px;
            height: 20px;
            position: relative;
            cursor: pointer;
        }

        .hamburger span {
            display: block;
            position: absolute;
            height: 2px;
            width: 100%;
            background: black;
            transition: .25s ease-in-out;
        }

        .hamburger span:nth-child(1) { top: 0px; }
        .hamburger span:nth-child(2) { top: 9px; }
        .hamburger span:nth-child(3) { top: 18px; }

        .hamburger.active span:nth-child(1) {
            top: 9px;
            transform: rotate(45deg);
        }

        .hamburger.active span:nth-child(2) {
            opacity: 0;
        }

        .hamburger.active span:nth-child(3) {
            top: 9px;
            transform: rotate(-45deg);
        }
    </style>
</head>
<body class="bg-white">
    <!-- Modern Navbar -->
    <nav class="navbar-modern">
        <div class="container mx-auto px-6 py-4">
            <div class="flex items-center justify-between">
                <a href="/" class="text-2xl font-bold text-black">FinAid</a>
                
                <!-- Desktop Menu -->
                <div class="desktop-menu hidden md:flex items-center space-x-8">
                    <a href="/" class="text-gray-800 hover:text-black transition-colors">Home</a>
                    <a href="/features" class="text-gray-800 hover:text-black transition-colors">Features</a>
                    <a href="#apply" class="text-gray-800 hover:text-black transition-colors">Apply</a>
                    <a href="/login"><button class="btn-modern mt-4">Sign In</button></a>
                </div>

                <!-- Mobile Menu Button -->
                <div class="hamburger md:hidden">
                    <span></span>
                    <span></span>
                    <span></span>
                </div>
            </div>
        </div>
    </nav>

    <!-- Mobile Menu Overlay -->
    <div class="mobile-menu">
        <div class="flex flex-col items-center justify-center h-full space-y-8">
            <a href="/" class="text-white text-2xl">Home</a>
            <a href="/features" class="text-white text-2xl">Features</a>
            <a href="#apply" class="text-white text-2xl">Apply</a>
            <a href="/login"><button class="btn-modern mt-4">Sign In</button></a>
        </div>
    </div>

    <div class="main-content">
    <!-- Hero Section -->
    <section class="min-h-[60vh] bg-black text-white flex items-center justify-center relative overflow-hidden">
        <div class="absolute inset-0 opacity-20">
            <div class="floating" style="animation-delay: 0s; left: 10%; top: 20%;">⚪</div>
            <div class="floating" style="animation-delay: 0.5s; left: 20%; top: 60%;">⚪</div>
            <div class="floating" style="animation-delay: 1s; left: 80%; top: 30%;">⚪</div>
        </div>
        <div class="container mx-auto px-6 text-center relative z-10">
            <h1 class="text-5xl md:text-7xl font-bold mb-6">Powerful Features</h1>
            <p class="text-xl md:text-2xl text-gray-300 max-w-3xl mx-auto">
                Discover how our platform revolutionizes the financial aid process
            </p>
        </div>
    </section>
</div>

    <!-- Main Features Grid -->
    <section class="py-20 bg-gray-50">
        <div class="container mx-auto px-6">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 feature-grid">
                <div class="feature-card">
                    <div class="feature-icon">
                        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/>
                        </svg>
                    </div>
                    <h3 class="text-xl font-bold mb-4">Smart Application</h3>
                    <p class="text-gray-600">Intelligent form filling with real-time validation and suggestions based on your profile.</p>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">
                        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M22 12h-4l-3 9L9 3l-3 9H2"/>
                        </svg>
                    </div>
                    <h3 class="text-xl font-bold mb-4">Real-time Tracking</h3>
                    <p class="text-gray-600">Monitor your application status and receive instant updates on your financial aid journey.</p>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">
                        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
                        </svg>
                    </div>
                    <h3 class="text-xl font-bold mb-4">Secure Processing</h3>
                    <p class="text-gray-600">Bank-level encryption and security measures to protect your sensitive information.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Progress Stats Section -->
    <section class="py-20 bg-white">
        <div class="container mx-auto px-6">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-12">
                <div>
                    <h2 class="text-3xl font-bold mb-8">Platform Statistics</h2>
                    
                    <div class="mb-6">
                        <div class="flex justify-between mb-2">
                            <span class="font-semibold">Application Success Rate</span>
                            <span class="text-gray-600">95%</span>
                        </div>
                        <div class="progress-bar">
                            <div class="progress-bar-fill" style="width: 95%"></div>
                        </div>
                    </div>

                    <div class="mb-6">
                        <div class="flex justify-between mb-2">
                            <span class="font-semibold">User Satisfaction</span>
                            <span class="text-gray-600">98%</span>
                        </div>
                        <div class="progress-bar">
                            <div class="progress-bar-fill" style="width: 98%"></div>
                        </div>
                    </div>

                    <div class="mb-6">
                        <div class="flex justify-between mb-2">
                            <span class="font-semibold">Processing Speed</span>
                            <span class="text-gray-600">92%</span>
                        </div>
                        <div class="progress-bar">
                            <div class="progress-bar-fill" style="width: 92%"></div>
                        </div>
                    </div>
                </div>

                <div class="grid grid-cols-2 gap-6">
                    <div class="text-center">
                        <div class="animated-number">15K+</div>
                        <p class="text-gray-600">Students Helped</p>
                    </div>
                    <div class="text-center">
                        <div class="animated-number">50M+</div>
                        <p class="text-gray-600">Aid Secured</p>
                    </div>
                    <div class="text-center">
                        <div class="animated-number">200+</div>
                        <p class="text-gray-600">Partner Schools</p>
                    </div>
                    <div class="text-center">
                        <div class="animated-number">24/7</div>
                        <p class="text-gray-600">Support Available</p>
                    </div>
                </div>
            </div>
        </div>
    </section>


    <!-- Application Timeline Section -->
    <section class="py-20 bg-gray-50">
        <div class="container mx-auto px-6">
            <h2 class="text-3xl font-bold text-center mb-12">Application Timeline</h2>
            <div class="max-w-3xl mx-auto space-y-8">
                <div class="timeline-item flex items-start">
                    <div class="w-12 h-12 bg-black text-white rounded-full flex items-center justify-center font-bold mr-4">1</div>
                    <div>
                        <h3 class="text-xl font-bold mb-2">Create Your Profile</h3>
                        <p class="text-gray-600">Sign up and complete your basic profile information to get started.</p>
                    </div>
                </div>
                <div class="timeline-item flex items-start">
                    <div class="w-12 h-12 bg-black text-white rounded-full flex items-center justify-center font-bold mr-4">2</div>
                    <div>
                        <h3 class="text-xl font-bold mb-2">Submit Documents</h3>
                        <p class="text-gray-600">Upload required financial documents and academic information.</p>
                    </div>
                </div>
                <div class="timeline-item flex items-start">
                    <div class="w-12 h-12 bg-black text-white rounded-full flex items-center justify-center font-bold mr-4">3</div>
                    <div>
                        <h3 class="text-xl font-bold mb-2">Review & Submit</h3>
                        <p class="text-gray-600">Review your application and submit it for processing.</p>
                    </div>
                </div>
                <div class="timeline-item flex items-start">
                    <div class="w-12 h-12 bg-black text-white rounded-full flex items-center justify-center font-bold mr-4">4</div>
                    <div>
                        <h3 class="text-xl font-bold mb-2">Track Progress</h3>
                        <p class="text-gray-600">Monitor your application status and receive updates in real-time.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Call to Action Section -->
    <section class="py-20 bg-black text-white">
        <div class="container mx-auto px-6 text-center">
            <h2 class="text-4xl font-bold mb-6">Ready to Start Your Journey?</h2>
            <p class="text-xl text-gray-300 mb-8">Join thousands of students who have successfully secured their financial aid through our platform.</p>
            <button class="bg-white text-black px-8 py-3 rounded-full font-bold hover:bg-gray-100 transition-colors">
                Apply Now
            </button>
        </div>
    </section>

    <script>
        // Mobile menu functionality
        const hamburger = document.querySelector('.hamburger');
        const mobileMenu = document.querySelector('.mobile-menu');

        hamburger.addEventListener('click', () => {
            hamburger.classList.toggle('active');
            mobileMenu.classList.toggle('active');
        });

        // Close mobile menu on link click
        document.querySelectorAll('.mobile-menu a').forEach(link => {
            link.addEventListener('click', () => {
                hamburger.classList.remove('active');
                mobileMenu.classList.remove('active');
            });
        });

        // Navbar scroll effect
        window.addEventListener('scroll', () => {
            const navbar = document.querySelector('.navbar-modern');
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });
        // Existing animations
        const animatedNumbers = document.querySelectorAll('.animated-number');
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, { threshold: 0.5 });

        animatedNumbers.forEach(number => observer.observe(number));

        // Progress bar animation
        const progressBars = document.querySelectorAll('.progress-bar-fill');
        const progressObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const targetWidth = entry.target.style.width;
                    entry.target.style.width = '0';
                    setTimeout(() => {
                        entry.target.style.width = targetWidth;
                    }, 100);
                }
            });
        }, { threshold: 0.5 });

        progressBars.forEach(bar => progressObserver.observe(bar));

        // New animations for statistics
        const statCards = document.querySelectorAll('.stat-card');
        const statObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, { threshold: 0.5 });

        statCards.forEach(card => statObserver.observe(card));

        // Testimonial animations
        const testimonials = document.querySelectorAll('.testimonial-card');
        const testimonialObserver = new IntersectionObserver((entries) => {
            entries.forEach((entry, index) => {
                if (entry.isIntersecting) {
                    setTimeout(() => {
                        entry.target.classList.add('visible');
                    }, index * 200);
                }
            });
        }, { threshold: 0.5 });

        testimonials.forEach(testimonial => testimonialObserver.observe(testimonial));

        // Timeline animations
        const timelineItems = document.querySelectorAll('.timeline-item');
        const timelineObserver = new IntersectionObserver((entries) => {
            entries.forEach((entry, index) => {
                if (entry.isIntersecting) {
                    setTimeout(() => {
                        entry.target.classList.add('visible');
                    }, index * 200);
                }
            });
        }, { threshold: 0.5 });

        timelineItems.forEach(item => timelineObserver.observe(item));
    </script>
</body>
</html>